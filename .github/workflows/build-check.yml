name: Build Check

# Runs on every push to master and every PR.
# Compiles the app and runs all tests — does NOT create a release.
on:
  push:
    branches: [master, main]
    paths-ignore:
      - '**.md'
      - '.github/ISSUE_TEMPLATE/**'
  pull_request:
    branches: [master, main]

permissions:
  contents: read

jobs:
  check:
    name: Check (Windows)
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
          cache-on-failure: true

      - name: Install frontend dependencies
        run: npm ci

      - name: Frontend build + leak gate
        run: |
          npm run build
          npm run leakcheck

      - name: Rust tests
        working-directory: src-tauri
        run: cargo test --all
        env:
          CARGO_INCREMENTAL: '1'

      - name: Rust lint
        working-directory: src-tauri
        run: cargo clippy -- -D warnings

  check-linux:
    name: Check (Linux)
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
          cache-on-failure: true

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libpipewire-0.3-dev \
            patchelf

      - name: Install frontend dependencies
        run: npm ci

      - name: Frontend build + leak gate
        run: |
          npm run build
          npm run leakcheck

      - name: Rust tests
        working-directory: src-tauri
        run: cargo test --all
        env:
          CARGO_INCREMENTAL: '1'

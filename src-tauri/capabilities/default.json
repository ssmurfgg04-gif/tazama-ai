{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Tazama AI - core window permissions plus global-shortcut registration. Plugin IPC grants (log, store, keyring) removed: the frontend calls only custom app commands and never invokes plugin:* directly. Rust-side plugin calls are unaffected by capability entries.",
  "windows": ["main", "hud"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-is-registered"
  ]
}

